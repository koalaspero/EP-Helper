const AddUser = ({ data = [], fileName }) => {
  return (
    <button
      className="add-btn ml-auto"
      onClick={() => {
      }}
    >
      Registrar Usuarios
    </button>
  );
};

export default AddUser;